import { Vue } from 'vue-property-decorator';
export default class InputButton extends Vue {
    private label;
    private icon;
    private iconPosition;
    private mounted;
    private readonly onlyIcon;
}
